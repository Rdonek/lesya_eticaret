import { View } from 'react-native';

// This screen is never rendered because the tab press is intercepted to open the menu modal.
export default function MoreScreenPlaceholder() {
  return <View />;
}
